var id = new URLSearchParams(location.search).get("id") || "alcazar_arquitectonico";

fetch("https://gevtsv.github.io/event/nfc/" + id + ".html")
  .then(e => e.text())
  .then(e => {
    document.open(),
    document.write(e),
    document.close()
  });
